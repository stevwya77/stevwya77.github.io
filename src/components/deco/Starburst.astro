---
/**
 * TODO(you) — Step 4: a Y2K starburst / sparkle, pinned to a corner of the front panel.
 *
 * Build it as an inline <svg> (a 4- or 8-point star is ~one <path>), then:
 *   - position: absolute; inset-block-start / inset-inline-end to park it
 *   - fill it with the --pearl gradient (hint: an SVG <linearGradient>, or CSS mask + background)
 *   - optional: a slow `rotate` animation, wrapped in @media (prefers-reduced-motion: no-preference)
 *
 * Until then it renders nothing.
 */
---
